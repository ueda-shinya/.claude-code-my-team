# テンプレートディレクトリ：URL 直叩きを禁止し、PHP からの require 経由でのみアクセス可能とする
# Apache 2.4 対応 + 2.2 フォールバック両対応
Options -Indexes
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
