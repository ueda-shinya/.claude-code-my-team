# Notion CRM スコープ記述書

作成日：2026-03-26
作成者：アスカ（シンヤさんとの設計フェーズ合意内容をまとめたもの）

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| **プロジェクト名** | Notion 顧客リスト管理システム（Notion CRM） |
| **目的** | 営業・受注・既存顧客の情報を一元管理し、フォロー漏れ・商談ロストを防ぐ |
| **対象** | officeueda の全顧客（Web制作・AI事業共通） |
| **担当** | シンヤさん（運用） / アスカ（スクリプト実装・保守） |

---

## 1-2. 管理No.の運用ルール

| ルール | 内容 |
|---|---|
| **発行タイミング** | 見積書・請求書・領収書の発行が必要になったとき |
| **発行方法** | `python3 notion-crm.py issue-no` を実行 |
| **採番方式** | A→Z→AA→ZZ→AAA→ZZZ（重複チェック済みで自動採番） |
| **顧客追加時** | 管理No.は発行しない（空欄のまま登録） |
| **変更** | 一度発行した管理No.は変更不可（`update` コマンドから除外済み） |

> **理由**：顧客登録≠取引開始。請求が発生した段階で番号を確定させることで、番号の空き・重複・請求書との紐づけ管理がしやすくなる。発行後の変更を禁止することで、過去の請求書・見積書との紐づけが崩れないようにする。

---

## 1-3. Notion構造変更の運用ルール

| 操作 | 対応 | スクリプトへの影響 |
|---|---|---|
| フィールド名の変更 | **アスカに依頼** | 影響あり（名前で取得しているため） |
| フィールドタイプの変更 | **アスカに依頼** | 影響あり（送信形式が変わる） |
| フィールドの削除 | **アスカに依頼** | 影響あり（無音で保存されなくなる） |
| 新規フィールドの追加 | **アスカに依頼**（CLIから操作したい場合） | CLIから使いたい場合は要対応 |
| フィールドの並び替え | 自由にOK | **影響なし**（フィールド名で取得） |
| レコードの追加・編集 | 自由にOK | 影響なし |

> **理由**：スクリプトはフィールド名をキーにしてデータを取得・送信しているため、フィールド名・型・削除はスクリプトの動作に直結する。構造変更はアスカに依頼することで、スクリプトとNotionの整合性を保つ。

---

## 2. スコープ（やること）

### Phase 1：Notion DB 構築
- Notionワークスペースにデータベースを作成する
- 以下のフィールド構成でプロパティを設定する

### Phase 2：Python スクリプト実装
- `~/.claude/scripts/notion-crm.py` を作成する
- Notion API を使って、CLIから顧客情報をCRUDできるようにする

### Phase 3：運用開始
- 既存の顧客情報を手動または一括でインポートする
- 日常的な顧客管理フローに組み込む

---

## 3. フィールド構成（承認待ち）

| フィールド名 | 型 | 選択肢・備考 |
|---|---|---|
| 会社名 / 屋号 | タイトル（必須） | |
| 担当者名 | テキスト | |
| 電話番号 | 電話番号 | |
| メールアドレス | メール | |
| ステータス | セレクト | 見込み・商談中・成約・失注・既存 |
| 事業種別 | セレクト | Web制作・AI・その他 |
| 担当 | テキスト | 複数担当がいる場合を想定 |
| 最終連絡日 | 日付 | |
| 流入元 | セレクト | Instagram・紹介・HP問い合わせ・その他 |
| メモ | テキスト（長文） | 商談メモ・特記事項 |

> **シンヤさんへの確認事項**：上記フィールド構成に追加・変更があれば教えてください。確定後にDB作成に進みます。

---

## 4. スコープ外（やらないこと）

- メール自動送信・自動フォロー（別プロジェクト：メール自動化 Phase 2 で対応）
- Notionのビュー設計・フィルター設定（シンヤさんが手動で設定）
- 既存ツール（スプレッドシート等）からの自動マイグレーション

---

## 5. 前提条件・制約

| 項目 | 内容 |
|---|---|
| Notion アカウント | シンヤさん名義のワークスペースが存在すること |
| API トークン | Integration を作成し `secret_...` を取得済みであること |
| Python 環境 | `notion-client` ライブラリがインストール可能であること |

---

## 6. WBS（作業分解）

```
Notion CRM 構築
├── Phase 1: DB構築
│   ├── 1-1. フィールド構成の最終承認（シンヤさん）
│   ├── 1-2. Notionワークスペース・APIトークン確認
│   └── 1-3. データベース作成・プロパティ設定
│
├── Phase 2: スクリプト実装
│   ├── 2-1. notion-crm.py 設計（CRUD操作の定義）
│   ├── 2-2. 実装（追加・一覧表示・更新・検索）
│   ├── 2-3. サクラによるセキュリティレビュー
│   └── 2-4. 動作確認・シンヤさんによる受け入れテスト
│
└── Phase 3: 運用開始
    ├── 3-1. 既存顧客データのインポート
    └── 3-2. 運用フローの確認
```

---

## 7. 再開チェックリスト

再開時に以下を確認してから着手すること：

- [ ] フィールド構成の承認（変更があれば上記テーブルを更新）
- [ ] Notionアカウントへのアクセス確認
- [ ] APIトークン（`secret_...`）の取得・`~/.claude/.env` への追記
- [ ] `pip install notion-client` が通るか確認

---

## 8. 関連ファイル

- スクリプト（未作成）：`~/.claude/scripts/notion-crm.py`
- このスコープ：`~/.claude/clients/officeueda/reports/notion-crm-scope-260326.md`
- 見積・請求台帳 設計書：`~/.claude/clients/officeueda/reports/notion-ledger-design-260326.md`

> **補足**：CRM DBの`識別記号`フィールドは、見積・請求台帳の管理番号採番に使用される。詳細は台帳設計書を参照。
