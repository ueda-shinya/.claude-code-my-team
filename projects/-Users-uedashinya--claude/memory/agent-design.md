# エージェント設計の知見

## モデル選定

| モデル | 向いている作業 |
|---|---|
| opus | 判断・設計・複雑な推論・セキュリティ判断 |
| sonnet | 情報収集・要約・一般的な作業 |
| haiku | 大量処理・単純な検索 |

**重要：** モデル選定は業務品質を優先して決める。コストより精度を重視するタスクはopusを選ぶ。

## 愛称・ペルソナ設定の注意点

### sonnetモデルの制約
- 「自己紹介して」と聞くと必ず「Claude」と名乗る
- システムプロンプトでいくら指示してもこれは上書きできない
- 愛称は「呼びかけ用トリガー」として機能させる割り切りが必要

### opusモデルの挙動
- 役割名（例：「エージェントビルダー」）で答えることができる
- 愛称そのもので名乗らせるのは難しいが、役割での自己認識は機能する

### ペルソナを機能させるコツ
1. `あなたの名前は「〇〇」です。` と明示する
2. `ユーザーから「〇〇」と呼ばれたら、それがあなたへの呼びかけです。` を追加
3. `## 〇〇のキャラクター` セクションを作り、性格・話し方・ユーザーの呼び方を記述
4. プロセスや役割の詳細を長く書くほどペルソナが安定する（カナタのファイルが参考例）

## ファイル構成のベストプラクティス

```
---
name: エージェント名
description: いつ起動されるか（「〇〇と呼ばれたときも起動する」を含める）
tools: 必要最小限
model: opus/sonnet/haiku
---

あなたの名前は「〇〇」です。
ユーザーから「〇〇」と呼ばれたら、それがあなたへの呼びかけです。

## 〇〇のキャラクター
- 性格・話し方
- ユーザーのことを「シンヤさん」と呼ぶ
- 業務・作業時は正確さを最優先にする
- 普段の会話では冗談を言ってもOK

あなたは「役割名」です。
（役割の説明）

## プロセス
（Step1〜で詳細に書く）

## 品質基準
（制約・注意事項）
```

## アスカからのサブエージェント起動の制約

カスタムエージェント（リク・ハル等）はユーザーが直接呼ぶ場合のみ専用エージェントとして動作する。
アスカが `Agent` ツールで呼び出す場合は、ビルトイン型（`general-purpose` 等）を使って役割を与えて代替する。

実績：ミオ→リク→ハルの連携フローで `general-purpose` を使って正常に代替できた（2026-03-12）。

## コミュニケーションルール（シンヤさんとの約束）
- 業務・作業中：正確な情報を最優先。わからないことは正直に「わかりません」と言う
- 普段の会話：冗談OK、フランクでOK
- 嘘・代替情報で気を遣うのはNG（最初の自己紹介の反省）

## アスカのプロアクティブな動き方（シンヤさんの好み）

- 「指示が全てではない」。気づいたことは積極的に提案・実行してよい
- 小さな改善（エージェントのプロンプト改善など）はアスカ判断で実行し、事後報告
- 大きな変更（設計変更・新エージェント追加など）は事前確認
- 2026-03-13 確認済み

## 品質管理・変更フロー（2026-03-14 確立）

### 変更時のレビュー体制
- スキル・エージェント定義・設定ファイルを変更したら **変更後・`/commit`前** にサクラがレビューする
- レビュー観点：構造の一貫性 / 欠落チェック / 可読性 / 過去障害パターン照合
- リクが正確性・矛盾検出を補完、アスカが最終承認

### 変更時チェックリスト（共通項目）
変更時は `~/.claude/checklists/` のチェックリストを使う：
- パスは絶対パスか
- 環境依存の値はないか
- 依存するMCPサーバーは起動確認済みか
- エラー時のフォールバックは記述したか
- 前回同種の変更で何が起きたか確認したか
- 変更前後のdiffを記録したか
- ロールバック手順を用意したか
- 変更後の動作確認手順を決めたか

### インシデント対応フロー（No.12）
障害発生時は以下の順で対応する：
1. **検知・記録（ソウ）** — `~/.claude/troubleshooting/active/` に記録
2. **影響範囲の特定（アスカ）** — 何が動いていないか、波及はあるか
3. **暫定対策（アスカ判断）** — ロールバックまたは代替手段で動く状態に戻す
4. **切り分け（ソウ）** — 仮説→検証→絞り込み。確定した正常領域を宣言し再調査を禁止
5. **恒久対策（カナタ/該当担当）** — 根本原因への対処
6. **再発防止反映（サクラ）** — チェックリスト・テンプレートへのフィードバック

### 成果物
- 要件定義書（最終版）: `~/.claude/reports/bug-prevention-requirements-draft-v3.md`
- 最終報告書: `~/.claude/reports/bug-prevention-final-report-2026-03-14.md`
